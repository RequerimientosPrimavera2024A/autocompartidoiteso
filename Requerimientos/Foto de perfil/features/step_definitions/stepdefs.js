const { Given, When, Then } = require('cucumber');

Given('El usuario accede a la opción de editar perfil en la aplicación', function () {
  // Implementación de acceso a opción de editar perfil
});

When('El usuario elige tomar una nueva foto utilizando la cámara de su dispositivo', function () {
  // Implementación de tomar nueva foto desde cámara
});

Then('La aplicación guarda la nueva foto como foto de perfil del usuario', function () {
  // Implementación de guardar nueva foto como foto de perfil
});

When('El usuario selecciona una foto existente desde la galería de imágenes de su dispositivo', function () {
  // Implementación de seleccionar foto existente desde galería
});

Then('La aplicación guarda la foto seleccionada como foto de perfil del usuario', function () {
  // Implementación de guardar foto seleccionada como foto de perfil
});
