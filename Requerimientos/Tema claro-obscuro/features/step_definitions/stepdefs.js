const { Given, When, Then } = require('cucumber');

Given('El usuario abre la aplicación AutoCompartidoITESO', function () {
  // Implementación de apertura de la aplicación
});

Given('El usuario accede a la sección de configuración', function () {
  // Implementación de acceso a la sección de configuración
});

When('El usuario selecciona la opción para cambiar el tema a {string}', function (theme) {
  // Implementación de selección de tema
});

Then('La aplicación actualiza el tema de inmediato en todas las pantallas', function () {
  // Implementación de actualización de tema
});

Then('La aplicación cambia el tema de manera uniforme en todos los elementos de la interfaz', function () {
  // Implementación de cambio uniforme de tema
});

Given('El usuario ha cambiado previamente el tema de la aplicación', function () {
  // Implementación de tema previamente cambiado
});

When('El usuario cierra y vuelve a abrir la aplicación', function () {
  // Implementación de cierre y reapertura de la aplicación
});

Then('La aplicación muestra el tema seleccionado anteriormente', function () {
  // Implementación de mostrar tema anterior
});
